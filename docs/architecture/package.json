{
  "name": "forsenclaw-architecture",
  "version": "0.0.0",
  "private": true,
  "description": "Curated LikeC4 architecture model for forsenClaw.",
  "scripts": {
    "start": "likec4 start --host 0.0.0.0 --port 5173",
    "build": "likec4 build --output dist",
    "export:png": "likec4 export png --output exports",
    "check": "likec4 validate"
  },
  "devDependencies": {
    "likec4": "^1.57.0"
  }
}
